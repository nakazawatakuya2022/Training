@page
@model WebSampleApp.Pages.S13RazorTestModel
@{
}

@if (Model.Keyword != null)
{
    <p>入力されたキーワード：@(Model.Keyword)</p>
    <hr />
}

<form method="get">
    <div>
        <label asp-for="@(Model.Keyword)">キーワード：</label>
        <input asp-for="@(Model.Keyword)" />
    </div>
    <button type="submit">検索</button>
</form>
